# ===========================================
# MSMP Backend Configuration
# ===========================================

# -----------------
# Database
# -----------------
# MongoDB Atlas connection string
# Get yours at: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/msmp?retryWrites=true&w=majority

# -----------------
# JWT Authentication
# -----------------
# Generate a secure random string (min 64 characters)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-64-chars-replace-this-with-generated-secret

# Server Port
PORT=5000

# -----------------
# CORS Configuration
# -----------------
# Frontend URL (update after deploying frontend)
# Local: http://localhost:5173
# Production: https://your-app.vercel.app
CLIENT_URL=http://localhost:5173

# -----------------
# Telco Provider
# -----------------
# Supported providers: Vodacom, MTN, CellC
TELCO_PROVIDER=Vodacom

# -----------------
# Admin Dashboard
# -----------------
# Password for accessing admin analytics dashboard
ADMIN_PASSWORD=YourSecurePassword123!

# -----------------
# SMS Configuration (EasySendSMS)
# -----------------
# Get your API credentials at: https://www.easysendsms.app/
# REST API v1 integration

# Your EasySendSMS API key
EASYSENDSMS_API_KEY=your-api-key-here

# Sender name/number that appears on recipient's phone
# Max 11 characters for alphanumeric, or use a phone number
EASYSENDSMS_SENDER_ID=YourBrand

# SMS Mode
# Set to 'false' for testing (OTP logged to terminal, no SMS sent, no costs)
# Set to 'true' for production (real SMS sent via EasySendSMS)
SMS_ENABLED=false

# -----------------
# Environment
# -----------------
# Use 'development' to include OTP in API response (for testing)
# Use 'production' to hide OTP from API response (security)
NODE_ENV=development

# ===========================================
# Quick Setup Guide
# ===========================================
# 1. Copy this file: cp .env.example .env
# 2. Update MONGODB_URI with your MongoDB Atlas connection string
# 3. Generate JWT_SECRET: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 4. Set ADMIN_PASSWORD to a secure password
# 5. (Optional) Add EasySendSMS credentials for production SMS
# 6. Save and restart server: npm run dev